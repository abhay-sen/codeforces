#include <bits/stdc++.h>
using namespace std;
int main() {
    int testcases;
    cin >> testcases;
    int length[testcases],array[testcases][length[testcases]];
    
    for(int i=0;i<testcases;i++){
        cin >> length[i];
        for (int j = 0; j < length[i]; j++)
        {
            cin>>array[i][length[i]];
        }
        
    }
    for (int i = 0; i < testcases; i++)
    {
        
    }
    
return 0;
}